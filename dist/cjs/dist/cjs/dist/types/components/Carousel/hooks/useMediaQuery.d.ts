declare type propType = {
    xs: number;
    sm: number;
    md: number;
    lg: number;
    xl: number;
};
export declare const useMediaQuery: ({ xs, sm, md, lg, xl }: propType) => number;
export {};
